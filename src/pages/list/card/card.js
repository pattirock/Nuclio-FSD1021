import React from 'react';
import styles from './card.module.css';

const Card = () => (
  <div className={styles.container}>
    <div className={styles.info}>Beer</div>
  </div>
);

export default Card;
